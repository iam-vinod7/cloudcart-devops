apiVersion: v1
kind: Service
metadata:
  name: flask-notes
spec:
  selector:
    app: flask-notes

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer
